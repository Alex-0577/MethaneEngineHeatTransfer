# 液氧/甲烷发动机再生冷却传热分析系统

## 项目简介

本项目是一个专业的液氧/甲烷发动机再生冷却和膜冷却传热数值研究工具。系统集成了NASA CEA燃烧计算、REFPROP物性计算和真实发动机几何数据分析，为液体火箭发动机的热防护设计提供精确的数值模拟支持。

## 主要特性

### 🔥 核心功能
- **CEA燃烧计算集成** - 基于NASA CEA程序计算燃烧产物热力学性质
- **REFPROP物性计算** - 精确计算高温高压条件下的流体物性参数
- **真实几何支持** - 加载和分析真实发动机几何轮廓数据
- **轴向分布分析** - 计算发动机沿轴向的温度、压力和热流分布
- **冷却性能评估** - 评估再生冷却和膜冷却系统的性能

### 📊 分析能力
- 燃气侧对流换热系数计算（巴兹公式）
- 冷却剂侧换热系数计算
- 壁面热传导计算
- 膜冷却效率分析
- 热平衡方程求解
- 敏感性分析和安全裕度评估

## 系统要求

### 硬件要求
- 内存：至少8GB RAM
- 存储：至少1GB可用空间
- 处理器：Intel i5或同等性能以上

### 软件要求
- 操作系统：Windows 10/11, Linux, macOS
- Python 3.8或更高版本
- REFPROP 10.0或更高版本（可选，用于精确物性计算）

## 安装部署

### 1. 环境准备

首先克隆或下载项目文件到本地目录。

### 2. 安装Python依赖

```bash
pip install -r requirements.txt
```
### 3. REFPROP配置（可选）
如果需要使用精确的物性计算，需要安装REFPROP：

1. 从NIST官网获取REFPROP安装包
2. 安装到默认路径或指定路径
3. 在parameters.json中配置正确的路径

### 参数配置
复制或修改parameters.json文件，根据实际发动机参数进行调整。

## 快速开始

### 基本使用

```python
from MethaneEngineHeatTransfer import LOX_MethaneEngineHeatTransfer

# 创建分析实例
engine = LOX_MethaneEngineHeatTransfer(
    refprop_path="C:/Program Files (x86)/REFPROP",
    use_cea=True,
    engine_shape_file="engine_shape.txt"
)

# 设置几何参数
engine.set_geometric_parameters(
    d_throat=0.0625,    # 喉部直径[m]
    L_chamber=0.257,    # 燃烧室长度[m]
    delta_wall=0.002,   # 壁厚[m]
    # ... 其他参数
)

# 运行分析
results = engine.calculate_axial_distribution(
    T_gas=3400, P_chamber=10e6, c_star=1750,
    m_dot_coolant=4.5, T_coolant_in=110, P_coolant_in=18e6,
    num_segments=60
)
```

### 命令行运行
```bash
python MethaneEngineHeatTransfer.py
```
程序将自动加载参数文件并执行完整的传热分析。

文件结构
```
project/
├── main.py                 # 主程序入口
├── engine_analysis.py      # 发动机分析核心类
├── parameters.json         # 参数配置文件
├── engine_shape.txt        # 发动机几何数据
├── requirements.txt        # Python依赖
├── README.md              # 说明文档
└── log.txt                # 运行日志（自动生成）
```

### 输出结果
程序运行后将生成：

 - ​控制台输出​：关键参数和进度信息
 - ​日志文件​：详细的调试和计算日志
 - ​轴向分布数据​：每个截面的温度、压力、热流等参数
 - ​性能评估报告​：冷却系统整体性能指标

## 附录：参数文件说明

### parameters.json 参数详解

#### 1. 发动机几何参数 (engine_geometry)
```json
{
  "d_throat": "喉部直径 [m]",
  "L_chamber": "燃烧室长度 [m]", 
  "delta_wall": "内壁厚度 [m]",
  "b_channel": "冷却通道宽度 [m]",
  "t_fin": "肋片厚度 [m]",
  "h_fin": "肋片高度 [m]",
  "delta_fin": "通道高度 [m]"
}
```

#### 燃烧分析参数 (combustion_analysis)
```json
{
  "Pc_MPa": "燃烧室压力 [MPa]",
  "mixture_ratios": "分析的混合比范围",
  "eps_values": "喷管面积比分析范围"
}
```

#### 材料属性 (material_properties)
```json
{
  "thermal_conductivity_points": "材料热导率随温度变化点"
}
```

#### 运行条件 (operating_conditions)
```json
{
  "coolant_velocity": "冷却剂流速 [m/s]",
  "hydraulic_diameter": "水力直径 [m]"
}
```

#### 发动机条件 (engine_conditions)
```json
{
  "T_gas": "燃气温度 [K]",
  "P_chamber_MPa": "燃烧室压力 [MPa]",
  "c_star": "特征速度 [m/s]",
  "m_dot_coolant": "冷却剂质量流量 [kg/s]",
  "T_coolant_in": "冷却剂入口温度 [K]",
  "P_coolant_in_MPa": "冷却剂入口压力 [MPa]",
  "mixture_ratio": "设计混合比"
}
```

#### 计算设置 (calculation_settings)
```json
{
  "num_segments": "轴向分段数量",
  "max_allowable_temp": "最大允许壁温 [K]"
}
```

#### 文件路径 (file_paths)
```json
{
  "refprop_path": "REFPROP安装路径",
  "engine_shape_file": "发动机几何文件",
  "parameter_file": "参数文件路径"
}
```
#### 安全系数 (safety_factors)
```json
{
  "temperature_safety_margin": "温度安全裕度",
  "pressure_safety_margin": "压力安全裕度"
}
```
#### 液膜冷却 (film_cooling)
```json
"film_cooling": {
    "start_position": 0.05,
    "liquid_film_end": 0.15,
    "film_flow_ratio": 0.1
}
```
#### 图表导出设置 (plot_settings)
```json
"plot_settings": {
    "output_directory": "图表文件夹",
    "figure_size": [12, 8],
    "dpi": 300,
    "plot_format": "png",
    "generate_all_plots": true
}
```

### 技术支持
如遇到问题，请检查：

1. 参数文件格式是否正确
2. REFPROP路径配置是否准确
3. 几何文件是否存在且格式正确
4. 所有依赖库是否安装成功